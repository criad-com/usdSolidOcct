[project]
name = "usdsolidocct-kit"
version = "0.1.5"
description = "UsdSolid and OCCT bridge source checks"
requires-python = ">=3.11"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["testenv"]
python_files = ["test_*.py"]
